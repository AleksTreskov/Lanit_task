server:
  port: 8088
spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/personcar
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create
    generate-ddl: true